// Clean — default. Source Serif body, Inter headings, navy accent.
// Base already implements this look; this file is empty by design so the
// gallery shape is symmetric.
export const cleanTheme = ``;
